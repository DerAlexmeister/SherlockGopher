# Dockerfile for the frontend.

FROM golang:1.14 as build
WORKDIR /app 
ADD . . 
RUN GOOS=linux GOARCH=amd64 CGO_ENABLED=0 go build -o frontend . 

FROM alpine:latest
COPY --from=build /app/frontend /app/
COPY views /app/views
WORKDIR /app 
RUN ls -la 
EXPOSE 8080
ENTRYPOINT [ "./frontend" ] 
